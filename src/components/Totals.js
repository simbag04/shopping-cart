const Totals = (props) => {
    return (
        <div>
            <div>Total Items: {props.totalItems}</div>
            <div>Total Price: {props.totalPrice}</div>
        </div>
    )
}

export default Totals